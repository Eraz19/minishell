#include "error.h"
#include "expander_.h"
#include <stdbool.h>

t_error	path_name_expansion(t_expander *state)
{
	(void)state;
	return (error(ERR_NO));
}

t_error	substitutions(t_expander *state)
{
	(void)state;
	return (error(ERR_NO));
}
