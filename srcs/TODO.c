#include "error.h"
#include "expander_.h"
#include <stdbool.h>

/* ---------- TODO: shell ---------- */

t_error		shell_should_interrupt(void)
{
	// TODO
	return (error(ERR_INTERRUPTED));
}

/* ---------- TODO: expander ---------- */
